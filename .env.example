# Database connection string (Prisma)
# For local PostgreSQL: postgresql://user:password@localhost:5432/servants_prep?schema=public
# For Supabase: See Supabase dashboard > Settings > Database > Connection Pooling
DATABASE_URL="postgresql://user:password@localhost:5432/servants_prep?schema=public"

# Direct database connection (for migrations, bypassing connection pooler)
# Only needed if using connection pooling (like Supabase's pgBouncer)
DIRECT_URL="postgresql://user:password@localhost:5432/servants_prep"

# NextAuth.js Configuration
# Set to your app's URL (in production, use your actual domain)
NEXTAUTH_URL="http://localhost:3000"

# NextAuth.js Secret (REQUIRED)
# Generate a strong secret with: openssl rand -base64 32
# IMPORTANT: Change this in production!
NEXTAUTH_SECRET="your-secret-key-change-this-in-production"
