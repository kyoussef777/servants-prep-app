// Prisma is a type source only. No database client or server code reaches native bundles.
export type {
  AttendanceStatus,
  SundaySchoolLevel,
  SundaySchoolAuthority,
  UserRole,
  RoleTag,
} from "@prisma/client";
export type * from "./sunday-school";
